const HOME_DIR = process.env.HOME;
const CACHE_FILE_NAME = ".rebar-cli";
const DEFAULT_REGISTRY = 'https://registry.npmjs.org/';

module.exports = {
  HOME_DIR,
  CACHE_FILE_NAME,
  DEFAULT_REGISTRY
};
